<UserControl x:Class="Marvin.Controls.Demo.Views.IconsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:Marvin.Controls.Demo.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:IconsViewModel}">
    <ScrollViewer>
        <ItemsControl ItemsSource="{Binding ShapeTypes}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="10">
                        <Border DockPanel.Dock="Bottom" BorderBrush="DarkGray" BorderThickness="1"></Border>
                        <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Text="{Binding .}"></TextBlock>
                        <Path DockPanel.Dock="Left" HorizontalAlignment="Center" Eddie.Icon="{Binding .}" Width="50" Height="50" Fill="Black" Margin="10"/>
                    </DockPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
